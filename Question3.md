This uses a function to compute x^y. It iterates through a list of x,y pairs and skips computing any pair where y is negative.
The code then stores the computations of x^y in a new list and prints the value of x^y in that new list
