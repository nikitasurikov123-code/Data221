This code uses a function to convert seconds into hours, then minutes, then seconds again, and also tells you if it's "am" or "pm".
It uses a modulus to get the remainder of seconds to hours, and if that number is less than or greater than 0, that determines AM or PM.
